# Per-attendee Flyte config (endpoint + generated auth). Written to ~/.flyte by the
# steering setup; ignored here in case anyone runs against a checkout.
.flyte/

# Your scratchpad. Everything the agent builds today lands here.
work/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.uv/

# Parsed output / caches from the LlamaIndex module
outputs/
.cache/

# OS
.DS_Store
